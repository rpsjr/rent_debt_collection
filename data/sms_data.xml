<?xml version="1.0" ?>
<odoo>
    <data noupdate="1">
        <!-- REMOVIDO: sms_template_data_invoice_sent (D-3) -->
        
        <!-- D-1: Apenas para Reincidentes (Aviso de Bloqueio Antecipado) -->
        <record id="sms_template_data_invoice_pre_due_bad" model="sms.template">
            <field name="name">Invoice D-1 Bad Payer: Alert by SMS</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="body">Babur: ${object.partner_id.name.split()[0]}, boleto vence amanha. Evite o bloqueio imediato do veiculo pagando em dia.</field>
        </record>

        <!-- D+0: Vencimento Hoje (Bom Pagador) -->
        <record id="sms_template_data_invoice_due_date_good" model="sms.template">
            <field name="name">Invoice Due Date Good Payer: Alert by SMS</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="body">Babur: ${object.partner_id.name.split()[0]}, seu boleto vence hoje. Evite juros e multas pagando em dia!</field>
        </record>

        <!-- D+0: Vencimento Hoje (Reincidente - Bloqueio Iminente) -->
        <record id="sms_template_data_invoice_due_date_bad" model="sms.template">
            <field name="name">Invoice Due Date Bad Payer: Alert by SMS</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="body">Babur: ${object.partner_id.name.split()[0]}, boleto vence hoje. O nao pagamento pode causar bloqueio do veiculo amanha.</field>
        </record>

        <!-- D+1: Atraso (Bom Pagador) -->
        <record id="sms_template_data_invoice_overdue_1_good" model="sms.template">
            <field name="name">Invoice Overdue D+1 Good Payer: Alert by SMS</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="body">Babur: ${object.partner_id.name.split()[0]}, nao identificamos seu pagamento. Regularize para evitar bloqueio em 48h.</field>
        </record>

        <!-- D+2: Atraso Crítico (Bom Pagador - Aviso de Bloqueio para D+3) -->
        <record id="sms_template_data_invoice_overdue_2_good" model="sms.template">
            <field name="name">Invoice Overdue D+2 Good Payer: Alert by SMS</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="body">Babur: ${object.partner_id.name.split()[0]}, ultimo aviso. Regularize seu debito hoje para evitar o bloqueio do veiculo.</field>
        </record>
        
        <!-- Bloqueado / Atraso Geral -->
        <record id="sms_template_data_invoice_overdue_blocked" model="sms.template">
            <field name="name">Invoice Overdue Blocked: Alert by SMS</field>
            <field name="model_id" ref="account.model_account_move"/>
            <field name="body">Babur: ${object.partner_id.name.split()[0]}, sua locacao esta vencida. Regularize seus pagamentos para liberacao do veiculo.</field>
        </record>
    </data>
</odoo>
